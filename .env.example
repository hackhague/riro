# --- Sanity CMS configuration ---
# Project details can be found in manage.sanity.io
SANITY_PROJECT_ID=your-sanity-project-id
SANITY_DATASET=production
# Defaults to today's date when omitted; pin to avoid breaking schema changes.
SANITY_API_VERSION=2025-01-01
# Only used in the Workers runtime. Leave unset for local development if your dataset is public.
SANITY_READ_TOKEN=
# Token used when Next.js draft mode is enabled for previews.
SANITY_PREVIEW_TOKEN=
# Optional: customise where the embedded studio is served.
SANITY_STUDIO_BASE_PATH=/studio

# --- Other environment variables ---
# Copy your existing Supabase, analytics and email credentials here as needed.
